# Request Gateway

请求网关服务，支持请求登记、超时预算、批量路由、查询、快照、审计、导入导出和延迟报告

The service is implemented with the Go standard library and includes record management, queries, snapshots, batch operations, audit history, imports, exports, and reports.

Run all checks with:

```console
go test ./...
```
